Unused source variants. Nothing in ./assets is published unless a template
references it, so these sit here without adding weight to the built site.

The live thumbnail is static/images/projects/lumamanta-detection.webp — animated
files have to live in static/, since running them through Hugo's image pipeline
would flatten them to a single frame.
